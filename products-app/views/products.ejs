<!DOCTYPE html>
<html>
  <head>
    <title>Products App</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"/>
  </head>
  <body>
    <h1 class="text-center"><%= title %></h1>
    <% for(var i=0; i<products.length; i++) {%>
    <div class="container">
      <div class="row mt-2">
        <div class="col-12 mt-2">
          <div class="border rounded bg-warning p-3">
            <h3 class="d-inline"><%= products[i].name%></h3>
            <a href="/productdetails/<%= products[i]._id%>" class="btn btn-danger float-right">Details</a>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </body>
</html>
